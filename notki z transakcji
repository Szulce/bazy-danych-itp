BAZ DANYCH TRNSAKCJE

PO EDYCJI NA KONIE CWPISUJEMY COMMIT BO TRANZAKCJA TO TAKA PACZKA I MY PROWADZAMY DOPIERO PO WYKOANIU WSZTSTKICH
PO POLECENIACH DDL TO SIE DZIEJE AUTOMATYCZNEI

zachowac punkty bezpieczenstwa i zachowac savepointy (taki backup)

aby wycofac sie z transakcji wpisujemy rollback,ale po jego uzyciu usuwaja sie sie wszykie savepointy


nadawanie uprawnień obiektowych :

GRANT SELECT ON NAZWA_TABELI TO NAWZA_UZYTKOWNIKA;

NAZWA_WLASCICIELA.NAZWA_TABLEI // WYŚWIETLANIE PRZEZ OSOBE DOŁACZONĄ DO UPRAWNIEŃ

SELECT * from WOLAND.chce ;
insert into WOLAND.chce values (13,'magda',5);
commit;

revoke co on kto from kot;


UPDATE PRACOWNICY PRACOWNICY SET PENSJA = PENSJA +500
WHERE NAZWISKO = 'KOTARSKI';

SAVEPOINT S1;

UPDATE PRACOWNICY PRACOWNICY SET PENSJA = PENSJA +300
WHERE NAZWISKO = 'Dolny';

SAVEPOINT S2;

DELETE * FROM PRACOWNICY 
WHERE NAZWISKO='MAKOWSKI';

ROLLBACK S1;

SELECT *
FROM PRACOWNICY;

ROLLBACK S2;//TO NIE POWINNO ZADZIALAĆ

ROLLBACK;

COMMIT;

DELETE * FROM PRACOWNICY 
WHERE NAZWISKO='MAKOWSKI';

ROLLBACK S1;

ALTER TABLE ETAT
MODIFY NAZWA VARCHAR2(20);

ROLLBACK S1;



SET TRANSACTION ISOLATION LEVEL SERIALIZABLE; 

SELECT *
FROM PRACOWNICY;

