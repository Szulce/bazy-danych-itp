CREATE VIEW   WIDOK1
AS (SELECT IMIE,NAZWISKO,projekty.NAZWA AS PROJEKT
FROM  PRACOWNICY  INNER JOIN DZIALY using (ID_DZIALU)
LEFT JOIN PROJEKTY on (P_ID=PROJEKT)           
WHERE  DZIALY.NAZWA='Programisci');
                  
SELECT *
FROM WIDOK1;
